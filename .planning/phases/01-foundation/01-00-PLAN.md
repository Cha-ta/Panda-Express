---
phase: 01-foundation
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - PandaAutomator/PandaAutomator.xcodeproj/project.pbxproj
  - PandaAutomatorTests/CodeFormatterTests.swift
  - PandaAutomatorTests/ValidationTests.swift
  - PandaAutomatorTests/ViewModelTests.swift
  - PandaAutomatorTests/EmailPersistenceTests.swift
  - PandaAutomatorTests/EngineSetupTests.swift
  - PandaAutomatorTests/AntiDetectionTests.swift
autonomous: true
requirements: [FORM-01, FORM-02, FORM-03, FORM-04, AUTO-01, AUTO-02]

must_haves:
  truths:
    - "XCTest target PandaAutomatorTests exists in the Xcode project"
    - "xcodebuild test -scheme PandaAutomator runs without scheme/target errors"
    - "6 test stub files exist with placeholder XCTestCase classes that compile"
  artifacts:
    - path: "PandaAutomatorTests/CodeFormatterTests.swift"
      provides: "Test stub for FORM-01 formatCodeWithDashes behavior"
      contains: "class CodeFormatterTests: XCTestCase"
    - path: "PandaAutomatorTests/ValidationTests.swift"
      provides: "Test stub for FORM-03 isValid computed property"
      contains: "class ValidationTests: XCTestCase"
    - path: "PandaAutomatorTests/ViewModelTests.swift"
      provides: "Test stub for FORM-04 isRunning disables inputs"
      contains: "class ViewModelTests: XCTestCase"
    - path: "PandaAutomatorTests/EmailPersistenceTests.swift"
      provides: "Test stub for FORM-02 AppStorage email persistence"
      contains: "class EmailPersistenceTests: XCTestCase"
    - path: "PandaAutomatorTests/EngineSetupTests.swift"
      provides: "Test stub for AUTO-01 WKWebView window attachment"
      contains: "class EngineSetupTests: XCTestCase"
    - path: "PandaAutomatorTests/AntiDetectionTests.swift"
      provides: "Test stub for AUTO-02 anti-detection configuration"
      contains: "class AntiDetectionTests: XCTestCase"
  key_links:
    - from: "PandaAutomatorTests target"
      to: "PandaAutomator main target"
      via: "Host application dependency in Xcode project"
      pattern: "TEST_HOST"
---

<objective>
Create the XCTest target and 6 unit test stub files that Plan 01 and Plan 02 verification commands depend on.

Purpose: Wave 0 prerequisite — Plans 01 and 02 need `xcodebuild test -only-testing:PandaAutomatorTests/...` to pass, which requires the test target and stub files to exist before production code is written.
Output: PandaAutomatorTests XCTest target in the Xcode project with 6 compilable test stub files.
</objective>

<execution_context>
@C:/Users/danie/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/danie/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PandaAutomatorTests XCTest target and 6 test stub files</name>
  <files>
    PandaAutomator/PandaAutomator.xcodeproj/project.pbxproj,
    PandaAutomatorTests/CodeFormatterTests.swift,
    PandaAutomatorTests/ValidationTests.swift,
    PandaAutomatorTests/ViewModelTests.swift,
    PandaAutomatorTests/EmailPersistenceTests.swift,
    PandaAutomatorTests/EngineSetupTests.swift,
    PandaAutomatorTests/AntiDetectionTests.swift
  </files>
  <action>
    1. Create the Xcode project first (this plan runs before 01-01). Use `xcodebuild` or write the project.pbxproj manually to create:
       - Product name: PandaAutomator
       - Interface: SwiftUI
       - Language: Swift
       - Deployment Target: iOS 16.0
       - Bundle ID: com.pandaautomator.app
       - No SPM dependencies
       - Include a unit test target: PandaAutomatorTests

    2. If the Xcode project already exists (created by 01-01 running first in an earlier attempt), add the PandaAutomatorTests unit test target to the existing project instead.

    3. The test target must:
       - Be named `PandaAutomatorTests`
       - Use `XCTest` framework
       - Have `TEST_HOST` set to the PandaAutomator app (host application)
       - Be included in the PandaAutomator scheme's Test action

    4. Create 6 test stub files under `PandaAutomatorTests/` directory. Each file should:
       - Import XCTest
       - Import the main module with `@testable import PandaAutomator`
       - Define an XCTestCase subclass
       - Include one placeholder test that calls `XCTFail("Not yet implemented")` so the tests compile but visibly fail until production code is written

    Files and their purpose:

    a. `PandaAutomatorTests/CodeFormatterTests.swift` — FORM-01
       ```swift
       import XCTest
       @testable import PandaAutomator

       final class CodeFormatterTests: XCTestCase {
           func testFormatCodeWithDashesInsertsDashesEvery4Chars() {
               XCTFail("Not yet implemented — will test formatCodeWithDashes")
           }
           func testFormatCodeStripsExistingDashesBeforeReformatting() {
               XCTFail("Not yet implemented")
           }
           func testFormatCodeCapsAt24AlphanumericCharacters() {
               XCTFail("Not yet implemented")
           }
       }
       ```

    b. `PandaAutomatorTests/ValidationTests.swift` — FORM-03
       ```swift
       import XCTest
       @testable import PandaAutomator

       final class ValidationTests: XCTestCase {
           func testIsValidFalseWhenCodeTooShort() {
               XCTFail("Not yet implemented — will test isValid computed property")
           }
           func testIsValidFalseWhenEmailMissingAt() {
               XCTFail("Not yet implemented")
           }
           func testIsValidTrueWhen24CharsAndValidEmail() {
               XCTFail("Not yet implemented")
           }
       }
       ```

    c. `PandaAutomatorTests/ViewModelTests.swift` — FORM-04
       ```swift
       import XCTest
       @testable import PandaAutomator

       final class ViewModelTests: XCTestCase {
           func testIsRunningDefaultsFalse() {
               XCTFail("Not yet implemented — will test ViewModel state")
           }
           func testStartAutomationSetsIsRunningTrue() {
               XCTFail("Not yet implemented")
           }
       }
       ```

    d. `PandaAutomatorTests/EmailPersistenceTests.swift` — FORM-02
       ```swift
       import XCTest
       @testable import PandaAutomator

       final class EmailPersistenceTests: XCTestCase {
           func testEmailPersistsToUserDefaults() {
               XCTFail("Not yet implemented — will test email persistence")
           }
       }
       ```

    e. `PandaAutomatorTests/EngineSetupTests.swift` — AUTO-01
       ```swift
       import XCTest
       @testable import PandaAutomator

       final class EngineSetupTests: XCTestCase {
           func testSetupCreatesWebView() {
               XCTFail("Not yet implemented — will test WKWebView creation")
           }
       }
       ```

    f. `PandaAutomatorTests/AntiDetectionTests.swift` — AUTO-02
       ```swift
       import XCTest
       @testable import PandaAutomator

       final class AntiDetectionTests: XCTestCase {
           func testUserAgentContainsSafari() {
               XCTFail("Not yet implemented — will test anti-detection config")
           }
           func testWebdriverScriptInjectedAtDocumentStart() {
               XCTFail("Not yet implemented")
           }
       }
       ```

    5. Verify the scheme includes the test target by running `xcodebuild -list` and confirming PandaAutomatorTests appears.

    IMPORTANT: These stubs intentionally fail. Plan 01 tasks will implement the production code that makes them pass, and Plan 01's verify commands will run them.
  </action>
  <verify>
    <automated>cd PandaAutomator && xcodebuild build-for-testing -scheme PandaAutomator -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -5</automated>
  </verify>
  <done>
    PandaAutomatorTests target exists in the Xcode project. All 6 test stub files compile. `xcodebuild build-for-testing` succeeds (tests compile but will fail at runtime with XCTFail until production code is written).
  </done>
</task>

</tasks>

<verification>
- `xcodebuild -list` shows PandaAutomatorTests in the scheme
- `xcodebuild build-for-testing` succeeds without compilation errors
- All 6 test files exist under PandaAutomatorTests/
</verification>

<success_criteria>
- XCTest target compiles and is wired into the PandaAutomator scheme
- 6 stub test files exist with XCTestCase subclasses that compile
- Plan 01 can reference these test targets in its verify commands
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-00-SUMMARY.md`
</output>
