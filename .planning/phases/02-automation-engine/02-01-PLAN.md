---
phase: 02-automation-engine
plan: 01
type: execute
wave: 1
depends_on: [02-00]
files_modified:
  - PandaAutomator/PandaAutomator/Engine/JSSnippets.swift
  - PandaAutomator/PandaAutomator/Engine/PageHandler.swift
autonomous: true
requirements: [AUTO-03, AUTO-04, AUTO-05, AUTO-06, AUTO-07, AUTO-08, AUTO-09, AUTO-10]

must_haves:
  truths:
    - "All JavaScript snippets for form automation are defined as static constants/functions"
    - "Page elements can be classified into the correct page type based on element counts"
    - "JS snippets dispatch synthetic events after setting text values to satisfy form frameworks"
  artifacts:
    - path: "PandaAutomator/PandaAutomator/Engine/JSSnippets.swift"
      provides: "All JS string constants for DOM interaction"
      min_lines: 80
      contains: "enum JSSnippets"
    - path: "PandaAutomator/PandaAutomator/Engine/PageHandler.swift"
      provides: "PageElements struct, CheckboxInfo, PageType enum, classifyPage function"
      min_lines: 60
      contains: "struct PageElements"
  key_links:
    - from: "PandaAutomator/PandaAutomator/Engine/JSSnippets.swift"
      to: "script.py"
      via: "Ported selectors"
      pattern: "#NextButton|input\\[type=\"radio\"\\]|input\\[type=\"checkbox\"\\]"
    - from: "PandaAutomator/PandaAutomator/Engine/PageHandler.swift"
      to: "PandaAutomator/PandaAutomator/Engine/JSSnippets.swift"
      via: "classifyPage uses element counts from JSSnippets.detectPageElements output"
      pattern: "PageElements|classifyPage"
---

<objective>
Create the JS snippet constants and page classification types that the automation engine loop will consume. This is the "contracts and logic" layer -- all JavaScript strings ported verbatim from script.py, and the Swift types that decode and classify page state.

Purpose: Isolate all JS interaction strings and page dispatch logic into testable, reusable units before wiring them into the async engine loop.
Output: JSSnippets.swift (all JS constants) and PageHandler.swift (PageElements, PageType, classifyPage).
</objective>

<execution_context>
@/Users/danielvillasenor/.claude/get-shit-done/workflows/execute-plan.md
@/Users/danielvillasenor/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-automation-engine/02-RESEARCH.md
@.planning/phases/02-automation-engine/02-00-SUMMARY.md
@PandaAutomator/PandaAutomator/Engine/AutomationEngine.swift
@script.py

<interfaces>
<!-- From Phase 1 AutomationEngine (existing): -->
```swift
@MainActor
class AutomationEngine: NSObject {
    private var webView: WKWebView!
    var onLog: ((String) -> Void)?
    func setup()
    func loadFeedbackPage()
    func run(code: String, email: String)  // stub -- Plan 02 replaces
    var userAgent: String { get }
    var userScripts: [WKUserScript] { get }
}
```

<!-- From script.py (authoritative reference for selectors): -->
- get_page_elements(): returns radioCount, radioNames, checkboxCount, checkboxes, textInputCount, textInputs, textareaCount, textareas
- click_all_radios_first_option(): getElementById('{name}.{value}').click()
- fill_text_by_index(): query inputs by name, page.fill(selector, value)
- fill_all_text_inputs(): same pattern for email
- fill_all_textareas(): query by name, page.fill(selector, value)
- click_next(): selectors = ["#NextButton", "input[value='Next']", "button:has-text('Next')", ".NextButton"]
- is_finish_page(): no #NextButton + body text contains indicators
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create JSSnippets.swift with all JavaScript string constants</name>
  <files>PandaAutomator/PandaAutomator/Engine/JSSnippets.swift</files>
  <behavior>
    - JSSnippets.detectPageElements contains querySelectorAll for radio, checkbox, text, textarea and returns JSON.stringify
    - JSSnippets.isFinishPage checks for #NextButton absence and body text indicators, returns Bool
    - JSSnippets.clickNext tries #NextButton, input[value="Next"], .NextButton selectors, returns matched selector or null
    - JSSnippets.fillTextInput(name:value:) sets .value and dispatches input+change events, returns Bool
    - JSSnippets.clickRadio(name:value:) uses getElementById("{name}.{value}").click(), returns true
    - JSSnippets.clickCheckbox(id:) uses getElementById(id).click(), returns true
    - JSSnippets.fillTextarea(name:value:) sets .value and dispatches input+change events, returns Bool
    - JSSnippets.getRadioValues(name:) returns array of values for a radio group name
    - JSSnippets.fillTextByIndex(index:value:) fills the nth text input by index (for code entry chunks)
  </behavior>
  <action>
Create `PandaAutomator/PandaAutomator/Engine/JSSnippets.swift` as an `enum JSSnippets` (no cases, namespace only) with:

1. `static let detectPageElements: String` -- Port `get_page_elements()` from script.py lines 24-51 verbatim. The JS IIFE queries radio, checkbox, text input (not readonly), and textarea elements, collects their metadata, and returns `JSON.stringify({...})`. Must return a String result (not void) per Pitfall 1.

2. `static let isFinishPage: String` -- Port `is_finish_page()` from script.py lines 145-164. Check `#NextButton` absence AND body text contains any of: "your validation code", "thank you for completing", "survey complete", "your code will be emailed", "we appreciate your feedback". Returns Bool.

3. `static let clickNext: String` -- Port `click_next()` from script.py lines 130-143. Try selectors in order: `#NextButton`, `input[value="Next"]`, `.NextButton`. Return the matched selector string or `null`. NOTE: script.py also has `button:has-text('Next')` but `:has-text` is a Playwright-only pseudo-selector -- replace with `button` and check innerText contains "Next" in JS.

4. `static func fillTextInput(name: String, value: String) -> String` -- Set `input[name="..."].value` then dispatch `new Event('input', {bubbles: true})` and `new Event('change', {bubbles: true})` per Pitfall 4. Return true/false. Escape the value for JS string safety (replace single quotes with \').

5. `static func clickRadio(name: String, value: String) -> String` -- Use `document.getElementById('\(name).\(value)').click()`. Return true. Matches script.py line 74.

6. `static func clickCheckbox(id: String) -> String` -- Use `document.getElementById('\(id)').click()`. Return true. Matches script.py line 236.

7. `static func fillTextarea(name: String, value: String) -> String` -- Set `textarea[name="..."].value` then dispatch input+change events. Return true/false.

8. `static func getRadioValues(name: String) -> String` -- Query `input[type="radio"][name="..."]` and return `JSON.stringify(Array.from(radios).map(r => r.value))`. Matches script.py lines 63-68.

9. `static func fillTextByIndex(index: Int, value: String) -> String` -- Query all `input[type="text"]:not([readonly])`, access by index, set value, dispatch events. Return true/false. Matches script.py `fill_text_by_index()`.

All JS expressions MUST be wrapped in an IIFE `(() => { ... })()` and MUST return a value (never void) per Pitfall 1 from RESEARCH.md.

Add the file to the PandaAutomator target in project.pbxproj.
  </action>
  <verify>
    <automated>cd "/Users/danielvillasenor/Panda Express" && xcodebuild build -project PandaAutomator/PandaAutomator.xcodeproj -scheme PandaAutomator -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | tail -5</automated>
  </verify>
  <done>JSSnippets.swift exists with all 9 JS constants/functions. All JS IIFEs return a value. Selectors match script.py. Text setters dispatch input+change events. Project builds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create PageHandler.swift with element types and page classification</name>
  <files>PandaAutomator/PandaAutomator/Engine/PageHandler.swift</files>
  <behavior>
    - PageElements is Decodable and holds radioCount, radioNames, checkboxCount, checkboxes ([CheckboxInfo]), textInputCount, textInputs, textareaCount, textareas
    - CheckboxInfo is Decodable with name, value, id fields
    - classifyPage returns .codeEntry when pageNum==0 and textInputCount==6
    - classifyPage returns .radioYesNo when radioCount==1 (yes/no detection happens at runtime with getRadioValues)
    - classifyPage returns .radioSatisfaction when radioCount > 1
    - classifyPage returns .checkbox when checkboxCount > 0
    - classifyPage returns .textarea when textareaCount > 0
    - classifyPage returns .emailInput when textInputCount > 0 and textInputCount < 6
    - classifyPage returns .unknown when no elements match
    - Priority order matches script.py: code entry > radio > checkbox > textarea > email
  </behavior>
  <action>
Create `PandaAutomator/PandaAutomator/Engine/PageHandler.swift` with:

1. `struct CheckboxInfo: Decodable` -- fields: `name: String`, `value: String`, `id: String`.

2. `struct PageElements: Decodable` -- fields matching script.py get_page_elements() return: `radioCount: Int`, `radioNames: [String]`, `checkboxCount: Int`, `checkboxes: [CheckboxInfo]`, `textInputCount: Int`, `textInputs: [String]`, `textareaCount: Int`, `textareas: [String]`.

3. `enum PageType` -- cases: `codeEntry`, `radioSatisfaction`, `radioYesNo`, `checkbox`, `textarea`, `emailInput`, `unknown`.

4. `func classifyPage(elements: PageElements, pageNum: Int) -> PageType` -- Free function (not in a class). Logic mirrors script.py main loop (lines 199-245):
   - If pageNum == 0 AND elements.textInputCount == 6 -> `.codeEntry`
   - Else if elements.radioCount > 0:
     - If elements.radioCount == 1 -> `.radioYesNo` (runtime getRadioValues determines if truly yes/no, but classification is enough for dispatch)
     - Else -> `.radioSatisfaction`
   - Else if elements.checkboxCount > 0 -> `.checkbox`
   - Else if elements.textareaCount > 0 -> `.textarea`
   - Else if elements.textInputCount > 0 AND elements.textInputCount < 6 -> `.emailInput`
   - Else -> `.unknown`

5. Add a helper: `func chunkCode(_ code: String) -> [String]` -- Splits a 24-char code into 6 chunks of 4 characters. Used by the engine for code entry page.

Add the file to the PandaAutomator target in project.pbxproj.

After both files are created, run the test suite -- AutomationFlowTests and JSSnippetTests from Wave 0 should now compile and pass (GREEN state for those tests).
  </action>
  <verify>
    <automated>cd "/Users/danielvillasenor/Panda Express" && xcodebuild test -project PandaAutomator/PandaAutomator.xcodeproj -scheme PandaAutomator -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:PandaAutomatorTests/JSSnippetTests -only-testing:PandaAutomatorTests/AutomationFlowTests 2>&1 | tail -20</automated>
  </verify>
  <done>PageHandler.swift exists with PageElements, CheckboxInfo, PageType, classifyPage, and chunkCode. AutomationFlowTests and JSSnippetTests pass (GREEN). All page types from script.py are represented.</done>
</task>

</tasks>

<verification>
- JSSnippets.swift and PageHandler.swift exist in PandaAutomator/Engine/
- Both files are in PandaAutomator target in project.pbxproj
- `xcodebuild build` succeeds
- AutomationFlowTests and JSSnippetTests pass
- All JS selectors match script.py verbatim (except Playwright-only :has-text replaced with JS equivalent)
</verification>

<success_criteria>
All JS interaction strings are centralized in JSSnippets. Page classification logic matches script.py priority order. Wave 0 tests for these components pass GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/02-automation-engine/02-01-SUMMARY.md`
</output>
